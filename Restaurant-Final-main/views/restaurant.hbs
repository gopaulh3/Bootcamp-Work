<link rel="stylesheet" href="/css/restaurant.css">
<section id="title">
    <div class="container-fluid">
      <!-- Nav Bar -->
      <nav class="navbar navbar-expand-lg">
        <a class="navbar-brand" href="http://localhost:3000/restaurants">Reliverdoo</a>
        <button class="navbar-toggler custom-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item">
              <a class="nav-link" href="http://localhost:3000/restaurants#recyclerView">Restaurants</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="http://localhost:3000/restaurants/managing">Manage Restaurants</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#"">Contact</a>
            </li>
          </ul>
        </div>
      </nav>
      <h1 class="res-name">{{restaurant.name}}</h1>
    </div>

  </section>

  <!-- Restaurant Image -->
  <section id="res-image">
    <div class="filter-card">
      <div class="rec-img"><img src="{{restaurant.image}} alt=""></div>
    </div>
  </section>


      <!-- RecyclerView -->

      <section id="recyclerView">
        <div class="recyclerView-container">
          <div class="row" id="recyclerView-row">
            {{#each restaurant.Menu}}
   <div class="col-lg-6 col-sm-12 mcard">
  <h2 class="menu-title">{{this.title}}</h2>
      <div class="filter-card">
      <div class="table-wrapper-scroll-y my-custom-scrollbar">
      <table class="table mb-0 table-hover">
        <thead>
          <tr>
            <th scope="col">Food</th>
            <td scope="col">Price</th>
          </tr>
        </thead>
        <tbody>
          {{#each this.MenuItem}}
          <tr>
            <th scope="row">{{this.item}}</th>
            <td>£{{this.price}}</td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    
    </div>
      </div>
  </div>
  {{/each}}
          </div>
        </div>
    </section>
